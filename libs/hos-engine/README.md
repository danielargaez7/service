# hos-engine

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build hos-engine` to build the library.

## Running unit tests

Run `nx test hos-engine` to execute the unit tests via [Jest](https://jestjs.io).
