# geofence

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build geofence` to build the library.

## Running unit tests

Run `nx test geofence` to execute the unit tests via [Jest](https://jestjs.io).
